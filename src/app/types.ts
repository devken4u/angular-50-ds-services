export type Student = {
  name: string;
  year: string;
  section: string;
};

export type Employee = {
  name: string;
  work: string;
};

export type Fruit = {
  name: string;
};

export type Course = {
  code: string;
  name: string;
};

export type Book = {
  name: string;
  publishDate: string;
  author: string;
};

export type City = {
  city: string;
};

export type Movie = {
  name: string;
  rating: number;
};

export type CarModel = {
  name: string;
  year: number;
};
