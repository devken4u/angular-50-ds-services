export type Student = {
  name: string;
  year: string;
  section: string;
};

export type Employee = {
  name: string;
  work: string;
};

export type Fruit = {
  name: string;
};

export type Course = {
  code: string;
  name: string;
};
