export type Student = {
  name: string;
  year: string;
  section: string;
};

export type Employee = {
  name: string;
  work: string;
};

export type Fruit = {
  name: string;
};

export type Course = {
  code: string;
  name: string;
};

export type Book = {
  name: string;
  publishDate: string;
  author: string;
};

export type City = {
  city: string;
};

export type Movie = {
  name: string;
  rating: number;
};

export type CarModel = {
  name: string;
  year: number;
};

export type Product = {
  name: string;
  price: number;
};

export type Subject = {
  name: string;
  time: string;
};

export type Country = {
  name: string;
};

export type Sport = {
  name: string;
};
